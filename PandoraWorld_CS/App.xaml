<Application x:Class="PandoraWorld_CS.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PandoraWorld_CS"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        
        <!-- Globale Farbdefinitionen -->
        <Color x:Key="PrimaryColor">#FF4D8AB3</Color>
        <Color x:Key="SecondaryColor">#FF3D4547</Color>
        <Color x:Key="AccentColor">#FF2980B9</Color>
        <Color x:Key="BorderColor">#FF2C6A8A</Color>
        
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
        <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}" />
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />

        <SolidColorBrush x:Key="BoardGreen" Color="#FF2ECC40"/>
        <SolidColorBrush x:Key="BoardBlue" Color="#FF0074D9"/>
        <SolidColorBrush x:Key="BoardRed" Color="#FFFF4136"/>
        <SolidColorBrush x:Key="BoardYellow" Color="#FFFFDC00"/>
        <SolidColorBrush x:Key="BoardTransparent" Color="Transparent"/>

        <!-- Gerichtete Schatten-Effekte für Panels -->
        <!-- Obere Leiste: Schatten nach unten (270°) -->
        <DropShadowEffect x:Key="TopPanelShadow" 
                          BlurRadius="10" 
                          ShadowDepth="5" 
                          Opacity="0.4" 
                          Direction="270"/>
        
        <!-- Linke Leiste: Schatten nach rechts (0°) -->
        <DropShadowEffect x:Key="LeftPanelShadow" 
                          BlurRadius="10" 
                          ShadowDepth="5" 
                          Opacity="0.4" 
                          Direction="0"/>
        
        <!-- Untere Leiste: Schatten nach oben (90°) -->
        <DropShadowEffect x:Key="BottomPanelShadow" 
                          BlurRadius="10" 
                          ShadowDepth="5" 
                          Opacity="0.4" 
                          Direction="90"/>

        <!-- Rundumschatten für das Spielfeld -->
        <DropShadowEffect x:Key="BoardShadow" 
                          BlurRadius="20" 
                          ShadowDepth="0" 
                          Opacity="0.5" 
                          Color="Black"/>

        <!-- Button Style mit Hover-Effekt -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="0"
                                CornerRadius="5"
                                Padding="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
